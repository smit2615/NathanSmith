<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Nathan Smith", "Index", "Home", null, new { @class = "navbar-brand" })
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav">
                <li class="link" id="Home">@Html.ActionLink("Home", "Index", "Home", null, new { })</li>
                <li class="link" id="AboutMe">@Html.ActionLink("About Me", "AboutMe", "Home", null, new { })</li>
                <li class="link" id="Resume">@Html.ActionLink("Resume", "Resume", "Home", null, new { })</li>
                <li><a href="mailto:ns23@umail.iu.edu" class="btn btn-info">Email Me</a></li>
                <li id="output"></li>
            </ul>
        </div>
    </div>
</nav>

<script>
    $(document).ready(function () {
        $('ul.nav').children().removeClass('active');
        var pathname = window.location.pathname.substring(1);
        var identity;
        if (pathname.length === 0)
            identity = '#Home';
        else
            identity = '#' + pathname;
        $(identity).addClass('active');
    });
</script>
